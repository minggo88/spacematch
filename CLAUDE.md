# CLAUDE.md — SpaceMatch

## 소스 맵 (필수 참조)

**코드 수정 전 반드시 읽기**: [`docs/source-map.md`](docs/source-map.md)

이 파일에는 전체 소스 구조가 정리되어 있다:
- 모든 페이지 파일과 URL 매핑
- 모든 PHP API 모듈과 파일 목록
- Context / Hook / Component 역할
- 다국어 네임스페이스 구조
- 빌드·배포 경로

> 새 파일을 만들거나 기존 파일을 크게 변경했으면 `docs/source-map.md`도 갱신한다.

---

## 프로젝트 개요

**SpaceMatch** — 공간 호스트(Host)와 셀러(Seller) 간 B2B 매칭 플랫폼.  
**스택**: React 18 + Vite 5 + TailwindCSS (프론트) / PHP + MySQL (백엔드) / Cafe24 호스팅

**사용자 역할**: `superadmin` · `admin` · `host` · `seller` · `vendor`  
**지원 언어**: 11개 (`ko` · `en` · `en-CA` · `en-GB` · `fr-CA` · `ja` · `km` · `ru` · `th` · `uk` · `vi`)

---

## 파일 구조 (핵심 경로)

| 경로 | 용도 |
|------|------|
| `src/` | React 프론트엔드 소스 |
| `public/api/` | PHP 백엔드 API |
| `public/locales/` | 다국어 JSON 파일 |
| `dist/` | 빌드 결과물 (커밋 포함 — 배포용) |
| `docs/` | 배포 로그 + critical.md + source-map.md |
| `.env` | 환경변수·API 키 |

---

## 운영 원칙

1. **소스 맵 먼저** — 코드 작성 전 `docs/source-map.md`에서 관련 파일 위치 확인
2. **다국어 동기화** — 문자열 추가/수정 시 `public/locales/` 11개 언어 모두 수정
3. **Self-anneal** — 오류 시: 수정 → 테스트 → source-map 갱신 (유료 API는 먼저 사용자 확인)
4. **소스맵 갱신** — 새 파일·모듈 추가 시 `docs/source-map.md` 업데이트

---

## 코딩 원칙

### 1. 코딩 전 생각하기
- 가정을 명시적으로 진술하고 불확실하면 물어보기
- 여러 해석이 있으면 조용히 선택하지 말고 제시하기
- 더 간단한 접근법이 있으면 말하기; 혼란스러우면 멈추고 질문하기

### 2. 단순함 우선
- 문제를 해결하는 **최소한의 코드**만 작성
- 요청 이상의 기능·추상화·유연성 없음; 불가능한 시나리오 방어 없음
- 200줄로 썼는데 50줄로 가능했다면 → 다시 작성

### 3. 수술적 변경
- **필요한 것만** 건드리기 — 인접 코드·주석·서식 "개선" 금지
- 망가지지 않은 것 리팩터링 금지; 기존 스타일 유지
- 관련 없는 죽은 코드 발견 → 삭제 말고 언급
- 내 변경으로 생긴 미사용 import/변수/함수는 제거
- **규칙:** 변경된 모든 줄은 사용자 요청으로 직접 추적 가능해야 함

### 4. 목표 중심 실행
작업을 검증 가능한 목표로 변환:
- "버그 수정" → 재현 테스트 작성 후 통과
- "기능 추가" → 동작 확인 기준 먼저 정의

다단계 작업은 계획 먼저:
```
1. [단계] → 확인: [체크]
2. [단계] → 확인: [체크]
```

---

## Deploy 규칙

**코드를 수정했으면 반드시** `docs/DEPLOY_YYYY-MM-DD.md`에 기록한다.

| 언제 | 할 일 |
|------|-------|
| 날짜가 바뀔 때 | 당일 `DEPLOY_YYYY-MM-DD.md` 파일 생성 확인 |
| 코드 수정 후 | 증상·원인·조치·배포경로 기록 |
| PHP만 수정 | `public/api/` → `www/api/` FTP 업로드 |
| React 수정 | `npm run build` → `www/index.html` + `www/assets/` **같은 세트로** 업로드 |

> 규칙: PR·커밋만 하고 deploy 문서를 비우지 않는다.  
> 상세 형식: `docs/critical.md` 참고
