# Deploy — 2026-06-04

## 1. HTML 엔티티(&#039; &amp; 등) 전면 수정

**증상:** 사용자 이름, 설명, 커뮤니티 게시글/댓글 등에 `&#039;`, `&amp;` 같은 HTML 엔티티가 그대로 표시됨

**원인:**
- 쓰기 API들이 DB 저장 전 `htmlspecialchars()`를 적용 → 엔티티가 DB에 저장됨
- 읽기 API 대부분이 디코딩 없이 JSON 반환 → 프론트엔드에 엔티티 노출
- venues API만 예외적으로 `deep_decode()` 함수를 local 정의해 사용

**조치:**
1. `db_connect.php`에 전역 헬퍼 함수 3개 추가: `deep_decode()`, `decode_fields()`, `decode_rows()`
2. 쓰기 API에서 `htmlspecialchars()` 제거 (PDO prepared statements로 SQL injection 방지 충분)
   - `auth/register.php`
   - `users/update_profile.php`
   - `community/community_posts.php` (POST)
   - `community/community_comments.php` (POST)
3. 읽기 API에 `decode_fields()` 적용 (기존 DB 엔티티 데이터 복원)
   - `users/get_users.php`, `get_user_detail.php`, `browse_hosts.php`, `browse_sellers.php`, `get_public_profile.php`, `get_host_public.php`, `update_profile.php` (응답)
   - `applications/get_applications.php`
   - `chat/conversations.php`
   - `community/community_posts.php`, `community_comments.php` (GET)
4. venues API 3개 (`get_my_venues.php`, `get_all_venues_admin.php`, `get_pending_venues.php`)에서 중복 local `deep_decode()` 제거 → db_connect.php 전역 함수 사용

**파일:** `public/api/` + `dist/api/` 34개 파일

**배포:** PHP only → `www/api/` FTP 업로드

---

## 2. 벤더 가입 버튼 숨김

**내용:** `SignupSelection.jsx` — "벤더로 가입하기" 버튼 주석 처리 (UI에서 비노출)

**파일:** `src/pages/SignupSelection.jsx`

**배포:** `npm run build` → `www/` 업로드

---

## 3. 분석 페이지 카테고리·공간타입 한글 표시

**증상:**
- `/seller/analytics` 인기 카테고리 TOP 10 탭에서 카테고리 이름이 영어 키값('fashion', 'beauty' 등)으로 표시
- `/seller/popular` 공간 타입별 분포 탭에서 공간 타입이 영어('popup', 'fleamarket' 등)로 표시

**원인:** DB에 영문 키값이 저장되고, 프론트에서 번역 없이 그대로 렌더링

**조치:**
1. `src/pages/Analytics.jsx` — `useTranslation('auth')` 추가, `topCategories` 데이터를 `auth` 네임스페이스의 `categories.{key}`로 번역 후 HBarChart에 전달 (커스텀 카테고리는 원래 값 fallback)
2. `src/pages/seller/SellerPopularAlerts.jsx` — `getTypeLabel()` 헬퍼 추가, 타입 분포 차트 및 마켓 인사이트 텍스트에 적용

**파일:** `src/pages/Analytics.jsx`, `src/pages/seller/SellerPopularAlerts.jsx`

**배포:** `npm run build` → `www/` 업로드

---

## 4. 셀러 호스트 디렉토리 — 연락처 섹션 숨김

**증상:** 셀러 계정으로 `/seller/hosts`에서 호스트 모달을 열면 연락처 정보(이메일·전화·열람권 UI) 섹션이 노출됨.

**원인:** `SellerHostDirectory.jsx` 모달 내 Contact Info 블록에 역할 기반 조건이 없었음.

**조치:** Contact Info 블록 전체를 `user?.role !== 'seller'` 조건으로 감쌈. 셀러 계정에서는 연락처 텍스트·이메일·전화·잠금 UI 모두 미표시. 어드민 계정에서는 기존과 동일.

**파일:** `src/pages/seller/SellerHostDirectory.jsx`

**배포:** `npm run build` → `www/` 업로드
