# Deploy — 2026-06-03

## 1. 백그라운드 트래픽 최적화 (알림 폴링 간격 + 탭 비활성화 시 중단)

**증상:** 사이트에 접속만 해도 지속적인 HTTP 요청 발생  
**원인:**
- `DataContext.jsx`: 로그인 사용자 대상 알림 폴링이 10초 간격으로 상시 실행 (탭이 백그라운드여도 계속)
- `ChatPage.jsx`: 채팅 대화 선택 시 2초 간격으로 messages + conversations 2개 요청

**조치:**
1. `DataContext.jsx` 알림 폴링 간격 10초 → 30초로 변경
2. `DataContext.jsx` `visibilitychange` 이벤트 리스너 추가 — 탭이 숨겨지면 interval 중단, 복귀 시 즉시 1회 + 재시작
3. `ChatPage.jsx` `POLL_INTERVAL` 2000 → 3000ms로 변경

**효과:** 로그인 사용자 1명 기준 알림 요청 1시간 360회 → 120회 (66% 감소), 탭 백그라운드 시 0회

**파일:**
- `src/context/DataContext.jsx`
- `src/pages/ChatPage.jsx`

**배포:** `npm run build` → `www/index.html` + `www/assets/` 업로드

---

## 2. 소스 맵 문서 생성 및 CLAUDE.md 최신화

**내용:**
- `docs/source-map.md` 신규 생성 — 전체 소스 구조 문서화
  - 프론트엔드 페이지 전체 URL 매핑 (Admin/Seller/Host/Vendor/공개)
  - PHP API 모듈별 파일 목록 (20개 모듈, 130+ 파일)
  - Context/Hook/Component 역할 정리
  - 다국어 11개 언어 네임스페이스 구조
  - 빌드·배포 경로
- `CLAUDE.md` 업데이트 — 프로젝트 개요 추가, source-map.md 참조 원칙 추가

**파일:**
- `docs/source-map.md` (신규)
- `CLAUDE.md`
- `docs/DEPLOY_2026-06-03.md`

**배포:** git 커밋만 (빌드 불필요)
